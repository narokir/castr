<% if member_signed_in? %>

  <ul class="nav navbar-nav navbar-right ">
    <li class="nav-item dropdown bg-light border border-gray-500 rounded">
      <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
        <%# image_tag(profile_img, :alt => "member", :size => "22", class: "member-menu-thumb") %>
        <span class="ml-2"><%= icon("person-circle", class: "text-info", width:"20px", height:"20px") %></span> <%= current_member.first_name %>
      </a>
      <div class="dropdown-menu dropdown-menu-right" style="min-width: 250px;">
        <p class="px-4 mb-0 text-muted">
          <span class="font-weight-bold text-uppercase small"><%= current_member.first_name %> <%= current_member.last_name %></span>
          <span class="small d-block"><%= current_member.email %></span>
        </p>
      <div class="dropdown-divider"></div>
        <%= link_to "Your account", member_account_path, :class => "dropdown-item" %>
        <div class="dropdown-divider"></div>
        <%= link_to "Log Out", destroy_member_session_path, method: :delete, :class => "dropdown-item" %>
      </div>
    </li>
  </ul>

<% else %>

  <ul class="nav navbar-nav">
    <li class='nav-item'><%= link_to "Log In", new_member_session_path, :class => "nav-link"%></li>
    <li class='nav-item'><%= link_to "Sign Up", new_member_registration_path, :class => "btn btn-outline-action " %></li>
  </ul>
  
<% end %>


